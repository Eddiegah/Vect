# fibonacci.vect — classic recursive Fibonacci
# Demonstrates: functions, recursion, if/else, arithmetic

fn fib(n: int) -> int {
    if n <= 1 {
        return n
    }
    return fib(n - 1) + fib(n - 2)
}

var i = 0
while i <= 10 {
    print(fib(i))
    i = i + 1
}
